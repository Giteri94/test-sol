const clock=document.querySelector("#clock"),date=document.querySelector("#date"),zone=document.querySelector("#zone"),timeFormat=new Intl.DateTimeFormat(undefined,{hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:false}),dateFormat=new Intl.DateTimeFormat(undefined,{weekday:"long",day:"numeric",month:"long",year:"numeric"});if(zone)zone.textContent=Intl.DateTimeFormat().resolvedOptions().timeZone;function render(){const now=new Date();clock.textContent=timeFormat.format(now);clock.dateTime=now.toISOString();if(date)date.textContent=dateFormat.format(now)}render();setInterval(render,1000);
